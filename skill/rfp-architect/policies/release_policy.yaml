release_policy:
  required_checks:
    - format
    - structure
    - workflow_contract
    - lifecycle
    - lifecycle_state
    - eval_coverage
    - rfp_lint_selftest
    - release_evidence_traceability
  block_if:
    - has_todo
    - missing_owner
    - stale_audit
    - untraceable_release_evidence
